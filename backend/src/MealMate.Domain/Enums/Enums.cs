namespace MealMate.Domain.Enums;

public enum HouseholdRole { Admin, Member }

public enum IngredientCategory { Dairy, Vegetables, Meat, Fruit, Grains, Spices, Other }

public enum DietType { Regular, Vegetarian, Vegan, GlutenFree, Keto, Other }

public enum MealType { Breakfast, Lunch, Dinner, Snack }

public enum FridgeItemSource { Manual, Shopping, Receipt }

public enum ShoppingItemSource { Recipe, MustHave, Manual, Receipt }

public enum MuscleGroup { Chest, Back, Shoulders, Arms, Legs, Core, Cardio, FullBody }

public enum DayOfWeekEnum { Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday }
